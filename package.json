{
  "name": "rrhhmod-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "deploy": "npm run build"
  },
  "dependencies": {
    "hono": "^4.11.7",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0"
  }
}
