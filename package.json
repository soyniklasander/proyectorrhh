{
  "name": "rrhhmod-root",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && npm run prisma:generate",
    "build": "cd frontend && npm ci && npm run build",
    "deploy": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^2.4.3",
    "hono": "^4.11.7",
    "prisma": "^5.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260131.0",
    "@playwright/test": "^1.58.1"
  }
}
